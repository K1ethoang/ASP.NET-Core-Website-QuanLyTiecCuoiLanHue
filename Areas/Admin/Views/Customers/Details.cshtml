@model ASP.NET_Core_Website_QuanLyTiecCuoiLanHue.Models.Customer

@{
    ViewData["Title"] = "Chi tiết";
}


<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">@ViewData["Title"] / @Model.CustomerId</h1>
            </div>
        </div>
    </div>
    <!-- /.container-fluid -->
</div>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="card">
            <div class="card-body">
                <dl class="row mb-12">
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.CusName)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.CusName)
                    </dd>
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.PhoneNumber)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.PhoneNumber)
                    </dd>
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.Sex)
                    </dt>
                    <dd class="col-sm-10">
                        @if ((bool)Model.Sex)
                        {
                            @Customer.MALE
                        }
                        else
                        {
                            @Customer.FEMALE
                        }
                    </dd>
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.Address)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.Address)
                    </dd>
                    <dt class="col-sm-2">
                        @Html.DisplayNameFor(model => model.CitizenNumber)
                    </dt>
                    <dd class="col-sm-10">
                        @Html.DisplayFor(model => model.CitizenNumber)
                    </dd>
                </dl>

                <div>
                    <a asp-action="Edit" class="btn btn-outline-primary" asp-route-id="@Model.CustomerId">Chỉnh sửa</a> |
                    <a asp-action="Index" class="btn btn-outline-primary">Danh sách khách hàng</a>
                </div>
            </div>
        </div>
    </div>
    <!-- /.container-fluid -->
</section>
<!-- /.content -->